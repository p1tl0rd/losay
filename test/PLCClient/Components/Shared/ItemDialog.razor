@inject IItemService ItemService
@inject IDialogService DialogService
@using MudBlazor

<MudDialog>
    <DialogContent>
        <EditForm Model="itemDto" OnValidSubmit="Save">
            <MudGrid Spacing="2">
                <MudItem xs="12">
                    <MudTextField @bind-Value="itemDto.ItemCode" Label="Mã Item" Required RequiredError="Mã Item là bắt buộc" MaxLength="50" Variant="Variant.Outlined" />
                </MudItem>
                <MudItem xs="12">
                    <MudTextField @bind-Value="itemDto.ItemName" Label="Tên Item" Required RequiredError="Tên Item là bắt buộc" MaxLength="50" Variant="Variant.Outlined" />
                </MudItem>
                @*        <MudItem xs="12" sm="6">
                    <MudTextField @bind-Value="itemDto.Max" Label="Max Value" Required RequiredError="Max Value là bắt buộc" Variant="Variant.Outlined" InputType="InputType.Number" />
                </MudItem> *@
                @*     <MudItem xs="12" sm="6">
                  <MudTextField @bind-Value="itemDto.Min" Label="Min Value" Required RequiredError="Min Value là bắt buộc" Variant="Variant.Outlined" InputType="InputType.Number" />
                </MudItem>
                <MudItem xs="12">
                    <MudTextField @bind-Value="itemDto.TimeSamping" Label="Time Sampling (giây)" Required RequiredError="Time Sampling là bắt buộc" Variant="Variant.Outlined" InputType="InputType.Number" />
                </MudItem> *@
                <MudItem xs="12">
                    <MudCheckBox @bind-Value="itemDto.Status" T="bool" Label="Trạng thái hoạt động" Color="Color.Primary" />
                </MudItem>
            </MudGrid>
            <DataAnnotationsValidator />
        </EditForm>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Hủy</MudButton>
        <MudButton Color="Color.Primary" OnClick="HandleSubmit">Lưu</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter]
    private IMudDialogInstance MudDialog { get; set; }

    [Parameter]
    public ItemDto itemDto { get; set; } = new ItemDto();

    private async Task Save()
    {
        if (itemDto.Id == 0)
        {
            await ItemService.CreateItemAsync(itemDto);
        }
        else
        {
            await ItemService.UpdateItemAsync(itemDto);
        }
        MudDialog.Close(DialogResult.Ok(true));
    }

    private void Cancel()
    {
        MudDialog.Cancel();
    }

    private async Task HandleSubmit()
    {
        // MudForm không có Validate() khi dùng EditForm, ValidationMessage sẽ tự hiển thị
        await Save();
    }
}